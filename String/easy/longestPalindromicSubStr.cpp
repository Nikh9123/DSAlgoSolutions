#include<bits/stdc++.h>
using namespace  std ;

bool isPalindrom(string s)
{
  string temp = s ;
  reverse(temp.begin(), temp.end());
  return temp == s ;
}

string longestPalindrome(string s) {
  
}

int main()
{
  string s = "babad" ;
  string ans = 
}